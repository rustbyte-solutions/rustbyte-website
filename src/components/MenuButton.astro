---
export interface Props {
	text: string;
    href: string;
}

const { text, href } = Astro.props;
---

<a href={href} class="font-bold duration-500" id="menuLinkButton">
    <button>{text}</button>
</a>